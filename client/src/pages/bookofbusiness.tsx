import { Layout } from "@/components/layout";
import { BookOfBusinessView } from "./client-dashboard";

export default function BookOfBusinessPage() {
  return (
    <Layout>
      <BookOfBusinessView />
    </Layout>
  );
}
